# monitor-api

API central de observabilidade do Tribus Monitor.

## Endpoints

- `POST /checks` (Bearer)
- `GET /status`
- `GET /status/:serviceKey`
- `GET /incidents`
- `GET /history`
- `GET /services`
- `GET /health`

## Deploy

Cloudflare Workers (`wrangler.toml`).
