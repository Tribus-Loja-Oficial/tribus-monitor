# Referência — Rotas monitor-api

## Health

- `GET /health`

## Ingest

- `POST /checks` (Bearer token obrigatório)

## Consulta

- `GET /status`
- `GET /status/:serviceKey`
- `GET /incidents`
- `GET /history`
- `GET /services`
